# Repository workflow

- Keep `main` stable and use it for completed, reviewed work.
- Start future development from `main` on a branch named `feature/<short-description>`.
- Do not use the `codex/` branch prefix in this repository.
- Commit coherent increments consistently instead of accumulating unrelated changes.
- Run the relevant tests before committing.
