# Luna cat and moon cover

Final asset: `luna-cat-moon-cover-monochrome.png` (1024 × 1536 PNG).

Generated through the approved OpenAI Image API CLI fallback using `gpt-image-2`, high quality, and the exact prompt in `luna-cat-moon-cover-prompt.txt`. The generated original is retained as `luna-cat-moon-cover.png`. The final export removes the original’s faint cool tint to ensure strictly equal red, green, and blue channels.

Palette: black, charcoal, silver and grey. It complements Luna’s dark interface but does not include the UI’s sage-green accents.
